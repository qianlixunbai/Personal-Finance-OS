import type { ReactNode } from 'react';
export function Pagination({ page, totalPages, summary, previousDisabled, nextDisabled, onPrevious, onNext }: { page: number; totalPages: number; summary: ReactNode; previousDisabled: boolean; nextDisabled: boolean; onPrevious: () => void; onNext: () => void }) { return <nav className="pagination" aria-label="分页"><span className="pagination__summary">第 {page} / {totalPages} 页，{summary}</span><div className="pagination__actions"><button type="button" onClick={onPrevious} disabled={previousDisabled} className="pagination__button">上一页</button><button type="button" onClick={onNext} disabled={nextDisabled} className="pagination__button">下一页</button></div></nav>; }
