# v3.0 Phase 3C Transaction Import Preview & Validation Closing Review

## Scheduler lifecycle closure pass (2026-08-15)

- The supplied failure summary described a periodic cleanup task racing Spring context shutdown. The checked-out baseline did not yet contain that scheduler, so its latest baseline `clean test` was already green (`599` tests). This pass adds the missing production scheduler and closes the described lifecycle window before enabling it.
- The production scheduler is Spring-managed, enabled by default, and runs once per minute. It uses a dedicated single-thread `ThreadPoolTaskScheduler` that rejects shutdown work, waits up to 20 seconds for executor termination, and is stopped before its cleanup service dependency is destroyed.
- A service-level shutdown fence rejects new work and takes a write lock. Existing cleanup takes the corresponding read lock, so shutdown waits for an in-flight database cleanup to finish before dependency teardown; a callback that begins after the fence performs no database query.
- Generic `test` profile contexts disable autonomous periodic scheduling. The dedicated PostgreSQL integration test explicitly enables the scheduler and verifies its callback deletes both private payloads, marks the expired Session, and clears persisted references only through the normal cleanup path.
- Regression evidence after the lifecycle change: scheduler unit/lifecycle tests passed repeatedly; PostgreSQL scheduler cleanup integration passed; the real enabled-scheduler + in-flight cleanup + Spring Context shutdown regression passed; latest `mvnw.cmd -B clean test` completed with `603` tests, `0` failures, `0` errors, and `0` skipped tests. `git diff --check` also passed.
- Runtime cleanup exceptions remain observable: session-level failures are logged with their exception and retain persisted cleanup references for retry. The scheduler does not silently treat database failures as success.

## 结论

```text
v3.0 Phase 3C Transaction Import Preview & Validation：CLOSED — GO
```

- 最终独立只读复审：P0 = 0，P1 = 0。
- 后端干净回归：`599 tests`，failures/errors/skipped = `0/0/0`。
- 本结论仅覆盖 CSV/XLSX Preview、映射、校验、临时文件与 Session 生命周期；Confirm 金融写入和前端均为 `NOT STARTED`。

## 本阶段交付

- 受 JWT 用户域保护的 Preview API：格式与内容类型校验、CSV/XLSX 解析、映射更新、分页读取和取消。
- 服务端权威的 canonical row、字段校验、账户/类别可见性、文件内及数据库 probable-duplicate warning，以及 Preview plan/digest 绑定。
- Session 精确 15 分钟 TTL、expired/cancel fail-closed、完整映射失效旧 plan、用户隔离与私有 payload 清理。
- XLSX 安全限制：归档条目、展开大小、100:1 压缩比、外部关系、实体/XInclude、宏与嵌入对象拒绝；date/time 单元格不会静默丢失时间信息。
- V15 允许清理完成后的 session 清空临时 raw/plan reference；重复 EXPIRED 请求仍稳定返回 409。

## 金融边界与后续

- Preview 只写 Import Session 元数据和私有临时 payload/plan；不写 `transactions`，不变更账户余额、Account 或 Category。
- Confirm 的原子金融写入、receipt、幂等恢复与前端流程均未开始，必须作为后续独立阶段按 Phase 3A contract 实现。

## 验证证据

- `mvnw -B clean test`：599 tests，0 failures，0 errors，0 skipped。
- 清理 reference 的集成回归先验证 RED（V14 的 `NOT NULL` 阻止清空），再以 V15 验证 GREEN；重复 expired access 回归同样先 RED（400）后 GREEN（连续 409）。
- Sol 高思考最终只读复审：P0 = 0，P1 = 0，GO。
