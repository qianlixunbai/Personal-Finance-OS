# v3.0 Phase 3D Transaction Import Confirm Closing Review

## 结论

```text
Phase 3D Transaction Import Confirm
CLOSED — GO
```

最终独立 Closing Review：`GO — ready to close`

最终独立复验：Sol 高思考、窄范围、严格只读复验；P0 = 0，P1 = 0。

本 Closing Review 覆盖服务端普通流水导入 Confirm：`INCOME`、`EXPENSE` 与 `ADJUSTMENT`。本阶段不包含 Import Frontend、投资导入、`BUY` / `SELL` / `DIVIDEND` import、银行或券商 API、异步导入及移动端。

实现阶段初始 Closing 结论曾由独立只读复审否决：P0 = 0，P1 = 4。四项 P1 已完成最小修复并以真实 PostgreSQL 回归验证；随后第二次独立复审发现了 resultDigest timestamp canonicalization P1，最终以 V17 forward-only repair 完成 remediation，并经最终独立复验获得 GO。本文件记录完整历史，不将过去的 NO-GO 改写为“一直顺利通过”。

## 历史治理记录

以下历史问题和决策均保留为正式工程治理记录：

1. `Named Constraint Recovery Contract P1` 最终通过 Contract Amendment 澄清为 defense-in-depth fallback，不再作为正常同 Session 幂等的核心机制。
2. 首次独立 Closing Review 的四个 P1：
   - duplicate evidence Account-lock race window；
   - named constraint identity fail-closed；
   - Preview token security；
   - 10k multi-account / V15→V16 migration Gate。
3. 第二次独立 Closing Review 的 migration P1：V16 historical `resultDigest` timestamp canonicalization，典型差异为 `.123400Z` 与 `.1234Z`。
4. 最终采用 V17 forward repair：未修改已推送 V16，修复历史 digest canonicalization，并经最终独立 Sol Review 得到 GO。

## 冻结计划与金融权威

Confirm 只执行服务端冻结的 `PreviewPlan`。客户端不重新提交金融 rows 作为事实，不计算账户影响，也不决定余额。

`Transactions`、`Account.balance`、Batch、Items、Account Impacts 与 Session `CONSUMED` 位于同一 PostgreSQL transaction；任一步失败即整体 rollback。真实 PostgreSQL fault injection 已分别覆盖 Transaction、balance、Batch、Item、Account Impact 和 Session consume 六类写入点。

`AccountBalanceService` 仍是 production balance mutation 的权威路径。Import Confirm 没有形成第二套账户余额算法；Confirm 与手工 `Transaction` 并发已验证，最终余额与已提交的 transaction facts 一致。manual `TransactionService` 尚未完全复用 shared write rules 属于 P2 代码复用问题，不影响本阶段金融语义与锁定验证。

## 锁定、原子性与幂等

锁顺序固定为：

```text
Session FOR UPDATE
→ Account ID ascending
```

真实 PostgreSQL 验证覆盖 deterministic Confirm-vs-Confirm、Confirm-vs-Cancel、Confirm-vs-cleanup、Confirm-vs-manual Transaction、opposite Account ordering 以及 lock timeout。deterministic Confirm-vs-Confirm 通过 `pg_stat_activity` 确认第二请求确实等待 Session row lock；第一请求提交后，第二请求在锁内走 committed replay。

duplicate evidence 的最终 recheck 现已移动到 `Account ID ascending` locks 已取得之后；锁取得与最终 evidence 复核之间没有生产可写窗口。新增 Confirm-vs-manual Transaction 并发回归使用锁后 observer/latch：手工写入要么被 Account lock 阻塞到 Confirm 完成，要么先提交而导致 Confirm fail closed 为 evidence changed，不能静默消费旧证据。

- same key + same intent 返回同一 authoritative receipt；
- same key + different intent 返回 conflict；
- 同一 `Idempotency-Key` 跨用户隔离；
- rollback 不留下假 committed binding；
- committed replay 不重复金融写入。

unknown commit 的真实路径为：DB COMMIT 成功后 result delivery 失败，客户端结果未知；相同请求重试后按 committed Batch lookup 重建同一 authoritative receipt。验证结果为同一 Batch、同一 `resultDigest`、Transaction delta = 0、balance delta = 0。

receipt 是后端权威事实，从 committed persistence 重建，不依赖第一次 HTTP response 的内存对象；temporary `PreviewPlan` cleanup 后仍可 replay，`resultDigest` 可稳定重建。

## Named Constraint Recovery Contract Amendment

Implementation 过程中发现原 Named Constraint Recovery Gate 与正确的 Session serialization 模型存在冲突，因此经独立 Contract Amendment Review 后修订。

原理解要求正常同 Session 并发触发 named unique constraint 后 recovery；但同一 Session 的 Confirm 应先被 `Session FOR UPDATE` 串行，后到请求在锁内完成 committed lookup 并进入 replay/conflict resolution。故 Named Constraint Recovery 的最终定位为 defense-in-depth fallback，而不是正常同 Session 幂等的核心机制。

正常 exactly-once 由以下组合保证：

```text
Session FOR UPDATE
+ locked committed lookup
+ preallocated Batch identity
+ single DB transaction
+ DB constraints
```

fallback 只识别完整 PostgreSQL 身份 `(SQLSTATE=23505, schema=public, table=transaction_import_batches, constraint name)` 与当前 import 语义完全匹配的预期 constraint；successful replay 必须通过 authoritative committed persistence lookup，并完整匹配 user、Session、preallocated Batch、key 与 `requestHash`。没有可信 committed result 时 fail closed；exact duplicate 不得伪装为 successful replay；错误 SQLSTATE、schema、table、unrelated unique、FK、check 与 unknown named constraint 均不得 successful replay。正向 fallback、exact-duplicate authoritative check 与上述 negative matrix 均已验证。

完整 Amendment 历史保留于 [Phase 3D Transaction Import Confirm Contract](../design/V3.0-Phase3D-Transaction-Import-Confirm-Contract.md)，未删除、回滚或改写为旧 Gate。

## Preview、重复语义与用户隔离

Preview token 为 signed / opaque token。production 必须显式配置长度足够的 HMAC secret；不再存在公开默认 secret。token claims 绑定 `issuedAt`、Session `expiresAt`、canonical warning-set digest，以及 user / session / revision / plan identity；过期、时间字段畸形、warning set 不匹配、tampered token、cross-session token、stale token、foreign-user token 与 digest mismatch 均 fail closed。committed replay 在 token 到期后仍通过已提交 receipt 的 authoritative lookup 返回同一结果，不依赖 temporary `PreviewPlan`。

重复语义覆盖 probable duplicate warning、稳定 warning identity、acknowledgement、candidate added、candidate removed、unrelated transaction、exact duplicate，以及 forged / stale / foreign warning。evidence drift 必须重新 Preview，不能静默继续 Confirm。

已验证的用户域隔离至少包括 foreign Session、foreign Batch receipt、foreign Account、foreign Category、foreign preview token、foreign warning ID，以及跨用户相同 `Idempotency-Key`。

## 验收证据

### 10k acceptance

真实 10,000-row 验证结果：

```text
rows: 10000
total duration: ~31254 ms
DB transaction duration: 27978 ms
transactions: 10000
items: 10000
accounts: 1
final balance: -10000.00 CNY
replay delta: 0
```

DB financial transaction 明显低于冻结的 60 秒 P1 上限。receipt reconstruction duration 未单独记录，作为 P2 Observation，不伪造数字。

新增真实 10,000-row、100-account（每账户 100 rows）验收：全部 `Account Impact` 正确、100 个账户均为 `-100.00 CNY`、receipt impact 排序稳定、replay 无额外写入。完整回归中的该场景为 `30075 ms` total / `27881 ms` DB transaction；随后连续两轮并发/运行时回归的 DB transaction duration 分别为 `27.034s` 与 `27.073s`，均低于冻结的 60 秒 transaction 上限。

新增 V15 snapshot → V17 专项升级 Gate：在 V15 创建含 legacy consumed Batch/Item/Transaction、active Preview 与 cancelled Session 的代表性数据后经 V16、V17 升级，验证 Session 反向链接、`resultDigest`、Account Impact、active/cancelled 状态保留及 Batch immutable trigger。

### Runtime 与回归

真实 Spring Boot + PostgreSQL + Flyway V17 + JWT HTTP 验证覆盖 CSV、XLSX、Preview、Confirm、Receipt、Replay、Idempotency conflict、Cancelled、Expired 与 DB direct verification。

- 历史 remediation 回归曾为 640 tests、0 failures、0 errors、0 skipped；
- 完整最终回归：121 suites、641 tests、0 failures、0 errors、0 skipped，duration `520.307 s`。
- concurrency / runtime 核心集合：31 tests、0 failures、0 errors，连续两次通过（每轮约 1 分 51 秒）。
- rollback fault injection、unknown commit、deterministic Session lock replay、锁后 duplicate evidence recheck、constraint fallback / negative matrix、token expiry / warning digest / replay、V15→V17 upgrade、token / warning / isolation 与单账户及多账户 10k 均有上述真实验证证据。

## Test-only Hook 自查

post-commit unknown-commit injection、transaction duration observer 与 deterministic concurrency hooks / latches 均受测试边界控制：observer 不参与金融事务决策，hook 默认不启用，production runtime 不依赖 test hook；未保留 debug sleep、硬编码测试用户或绕过正常 locking 的路径。

## V17 resultDigest Timestamp Canonicalization Forward Repair

第二次独立只读复审确认的新增 P1 是：历史 Batch 的 `result_digest` 必须使用与 Java `Instant.toString()` 完全相同的 timestamp 字节表示；时间语义相同不足以保证 SHA-256 输入相同。

本轮未修改已推送的 V16。仓库存在 `origin/zh-cn` 上的 V16，且 Compose 配置使用持久 `postgres_data` volume；没有证据能够排除已经执行 V16 的不可重建持久数据库。因此采用 `V17__repair_transaction_import_receipt_digest_timestamps.sql` 前向修复：仅当历史 `result_digest` 与从 immutable Session、Batch、Items 和 Account Impacts 重建出的权威输入不一致时更新该行。migration 在同一 Flyway transaction 内临时禁用并恢复 Batch immutable trigger；没有修改 V16 checksum、没有 Flyway repair，也没有重写已推送历史。

Canonical timestamp contract：PostgreSQL `TIMESTAMP WITH TIME ZONE` 数据库路径可表达秒、毫秒或微秒。UTC 输出必须分别为：整秒无小数；纳秒可被 `1_000_000` 整除时固定 3 位毫秒；其余可表达的微秒固定 6 位。该规则与 Java `Instant.toString()` 对相同 Instant 的 UTF-8 字节输入一致；不会 trim trailing zero。

### Final timestamp precision matrix

真实 Spring Boot + PostgreSQL + Flyway V15→V16→V17 专项回归保留 legacy consumed Session、preallocated Batch、Item、Transaction、Account Impact 及 ownership，并覆盖：

- `2026-08-01T00:01:00Z`
- `2026-08-01T00:01:00.123Z`
- `2026-08-01T00:01:00.123456Z`
- `2026-08-01T00:01:00.123400Z`
- `2026-08-01T00:01:00.123450Z`
- `2026-08-01T00:01:00.000001Z`

测试先注入历史错误的 `.1234Z` digest，并确认真实 Java receipt reconstruction fail closed 为 `IMPORT_CONFIRM_INCONSISTENT`；V17 后六种时间精度均由 `TransactionImportConfirmService.getReceipt` 重建成功，persisted digest 与 Java recomputed digest 相等，同 key Confirm replay 返回同一 authoritative receipt。现有 JWT HTTP runtime 回归亦新增 fresh Confirm 后的 `GET /api/v1/imports/transactions/batches/{importBatchId}` 断言，并确认 GET digest 与 Confirm/replay digest 相同。

本轮完整后端回归命令为 `cd backend; $env:JAVA_HOME='F:\ideajava\jdk21'; .\mvnw.cmd clean test`：

```text
suites: 121
tests: 641
failures: 0
errors: 0
skipped: 0
duration: 520.307 s (Surefire suite aggregate)
```

本轮最终 remediation 结论为：`GO — ready to close`。最终 Sol 高思考、窄范围、严格只读复验已确认 Phase 3D `CLOSED — GO`。

## Findings

```text
P0: 0
P1: 0
P2:
1. Session 在锁内发现过期时执行 markExpired 后立即抛异常，更新会随 transaction rollback；预检后、取得锁前恰好过期的竞态可能保留 PREVIEW_READY。
2. manual `TransactionService` 尚未完全复用 shared write rules，属于代码复用问题，不影响本阶段金融语义与锁定验证。
3. receipt reconstruction duration 未单独记录。
4. Docker Compose / `.env` 示例尚未传递 `FINANCE_IMPORT_CONFIRM_TOKEN_SECRET`；这是部署体验问题，production 缺少 secret 时安全 fail-fast，不回退到公开默认 secret。
P3: 0
```

独立复审确认的既有基础仍成立；本次 remediation 同时消除了四项 P1。receipt reconstruction duration 仍未单独记录，保留为 P2 performance observation；不以估算值替代实测值。

## 延后范围

- Transaction Import Frontend：NOT STARTED。
- 未开始投资 import、`BUY` / `SELL` / `DIVIDEND` import、银行 API、broker API、async import 或 mobile。
- `sites-demo` 未修改。

## 关闭准入

```text
Phase 3D Transaction Import Confirm
CLOSED — GO
```
