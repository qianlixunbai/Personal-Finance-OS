# v1.5 Deployment Readiness Closing Review

## 1. Goal

v1.5 adds a minimal, reproducible single-host deployment path without changing business models, database structure, Flyway migrations, or the frozen architecture. The target path is:

```text
build images -> Docker Compose -> PostgreSQL -> backend -> frontend Nginx -> browser
```

## 2. Delivered scope

- Added Spring Boot Actuator as the only new application dependency.
- Added a `prod` profile with required datasource properties, disabled Springdoc endpoints, health-only Actuator exposure, liveness/readiness groups, database-aware readiness, and graceful shutdown.
- Allowed only `/actuator/health` and `/actuator/health/**` through Security; business APIs and non-health Actuator paths remain authenticated.
- Added a PostgreSQL Testcontainers integration test for anonymous health probes, protected API boundaries, and hidden health details.
- Added multi-stage backend and frontend Dockerfiles plus narrow Docker ignore files.
- Added Nginx SPA fallback, same-origin `/api/*` forwarding, and host-accessible `/actuator/*` forwarding.
- Added Docker Compose for PostgreSQL 17, backend, frontend, backend readiness health, frontend health, and a named PostgreSQL volume.
- Added Secret templates and ignore rules. Real values are neither stored in Dockerfiles nor in Compose.
- Added a CI deployment-artifacts job that builds both images and parses Compose without registry login, push, deployment, new Secrets, or broader permissions.
- Added the deployment guide and documentation navigation.

## 3. Deployment topology

```text
Browser
  -> Frontend Nginx
      - React static files and SPA fallback
      - /api/* and /actuator/* -> Spring Boot backend
                                      -> PostgreSQL 17
```

The frontend still uses its existing relative `/api/v1` base URL. No CORS policy or Vite production API address was introduced.

## 4. Local verification evidence

| Check | Result |
|---|---|
| Backend test suite | 117 tests, 0 failures, 0 errors, 0 skipped |
| Health integration test | 2 tests, 0 failures, 0 errors |
| Frontend test | 9 tests passed |
| Frontend lint and production build | Passed |
| Backend Docker image | Built; final container user is `financeos` and contains no runtime Secret defaults |
| Frontend Docker image | Built from Node build stage to Nginx runtime stage |
| Compose config | Parsed with `docker/.env` copied from the committed placeholder template |
| Compose startup | PostgreSQL, backend, and frontend healthy |
| HTTP smoke | Home and `/accounts` deep link returned `200`; registration/login returned `200`; Dashboard returned `401` without JWT and `200` with JWT |
| Health smoke | Liveness/readiness returned `{"status":"UP"}` with no datasource URL, credentials, JWT Secret, stack trace, or exception details |
| Persistence | A user registered before ordinary `down` could log in after `up`; named volume `finance-os_postgres_data` persisted |
| Graceful shutdown | Backend stop completed in 0.58 seconds, emitted graceful-shutdown logging, and returned to healthy after restart |

The existing Vite build reports a chunk-size warning for the ECharts bundle. It is unrelated to container deployment and remains an out-of-scope optimization.

## 5. Remote CI evidence

The workflow now includes the local-equivalent deployment-artifacts checks:

```text
docker build -t finance-os-backend:ci ./backend
docker build -t finance-os-frontend:ci ./frontend
docker compose --env-file docker/.env.example -f docker/compose.yml config
```

The implementation commit range `ca998e0..e478bb9` was verified by GitHub Actions run [#29580604608](https://github.com/qianlixunbai/Personal-Finance-OS/actions/runs/29580604608) for implementation HEAD `e478bb95bfd1b06afc02b56bd060056921c3186e`.

| Job | Result | Verified work |
|---|---|---|
| Backend | Passed | Maven backend test suite: 117 tests, 0 failures/errors |
| Frontend | Passed | 9 tests, lint, and production build |
| Deployment artifacts | Passed | Backend image build, frontend image build, and `docker compose --env-file docker/.env.example -f docker/compose.yml config` |

The workflow did not add a registry login, image push, deployment step, Secret, permission, or trigger expansion. The linked run is the remote CI evidence for the implementation commits.

## 6. Security and scope confirmation

- No real Secret was committed.
- `AGENTS.md` was not modified, moved, staged, or committed.
- `Architecture.md` was not modified.
- No Flyway migration, database table, Entity, DTO, Service, or business API was modified.
- No `sites-demo` content was used.
- No dependency other than Actuator was added.
- Workflow permissions and triggers were not expanded.
- No Kubernetes, Redis, MQ, microservices, cloud resources, HTTPS, registry push, automatic deployment, external monitoring, or different-origin CORS support was added.

## 7. Known limitations

- No HTTPS, domain, cloud deployment, registry publishing, or automatic CD.
- No automated backups, restore workflow, high availability, external monitoring, or log platform.
- No browser E2E framework or full Compose startup in CI.
- No multi-origin frontend/backend deployment; the supported topology is same-origin Nginx proxying.

## 8. Final GO / NO-GO

- v1.5 Deployment Readiness: **GO**.
- Entering v2.0 Market Data: **GO**.
- Complete production operations capability: **NO-GO**; v1.5 intentionally provides only minimal single-host deployment readiness.
