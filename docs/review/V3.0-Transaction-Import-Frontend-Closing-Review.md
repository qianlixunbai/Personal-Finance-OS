# V3.0 Transaction Import Frontend — Closing Review

**状态：GO — Frontend Sub-phase D CLOSED**

**状态：GO — Transaction Import Frontend Ready for Final Independent Closing Review**

本文关闭 Transaction Import Frontend 的 A–D 实施阶段，不替代最终的 Sol 独立只读验收。

## 范围

- A：Upload 与 same-Session Mapping；
- B：server-paged Preview、校验和 Warning；
- C：Confirm、Idempotency、Recovery、lossless Receipt；
- D：真实浏览器 E2E、reload/并发硬化、移动端和回执展示分页。

Sub-phase C 曾经历多轮独立 NO-GO 与 P1 修复，最终已获 Sol 高思考独立验收 GO；本轮不隐藏该历史，并在其基础上完成 D 的真实浏览器边界。

## D 关键证据

### Receipt reload

- 真实 `/transactions/import/receipts/:batchId` `page.reload()` 每次重新 GET 权威回执；不依赖 React memory、Confirm response 或本地 Receipt 内容。
- `9007199254740993.01`、`-0.01`、`9007199254740993.00` 在 reload 前后保持精确字符串。
- reload 后 GET 401：只保存同用户的 batch route identity（不保存回执/金额），同用户登录后回到原 route 并再次 GET；零 Confirm POST。
- reload 后 GET 404：仅显示安全 not-found，零 Confirm POST。
- 101 条 transaction refs 只渲染当前 100 条，支持键盘可达的上一页/下一页及范围提示。

### Multi-tab Confirm

两张真实 Browser Page 在同一 Session、同一 frozen Preview 上并发触发 Confirm。Web Lock 与 shared pending 保证仅产生一个 frozen intent；获锁者可以是任一标签。所有捕获的 Confirm POST 只有一个，并断言唯一 `Idempotency-Key`、固定 path 与精确 `bodyJson`。该场景额外稳定运行 `20 / 20 PASS`。

### CSV / XLSX 全流程

CSV 与 XLSX 都覆盖：Upload → 同一 Session 两次 Mapping PUT → discovery → formal mapping → server Preview → warning 显式 acknowledgement → Confirm → Receipt → Receipt reload。测试断言：仅一次 upload、两次 PUT 都使用同一 Session、revision 后重新加载 rows/warnings、未确认 warning 不可 Confirm，以及完整 flow 中的转义服务端文本。

## 不变量复核

- 后端是余额、Preview、warning、duplicate 和 Receipt 的唯一权威；前端不做余额聚合、重复判断或 warning ID 生成。
- Confirm 只发送冻结 token 与后端 warning IDs；同一 intent 保持相同 key/path/body。
- committed 状态单调，unknown recovery 先 GET，只有符合 Contract 的 404 才能恢复同一 POST。
- Receipt 金额使用 lossless transport 的字符串，不转换为 JavaScript Number。
- 草稿、pending、Receipt 登录返回均绑定用户；用户切换、logout unresolved、四条 401 与 14 个 domain code regression 继续由浏览器套件覆盖。

## 最终验证

```text
npm test
74 / 74 PASS

npm run test:e2e:transaction-import
Run 1: 55 / 55 PASS
Run 2: 55 / 55 PASS

npx playwright test --config playwright.transaction-import.config.ts --grep "simultaneous two-page" --repeat-each=20
20 / 20 PASS

npm run lint
PASS

npm run build
PASS（保留现有 >500 kB Vite advisory）

git diff --check
PASS
```

## Findings

| 严重度 | 结论 |
| --- | --- |
| P0 | 0 |
| P1 | 0 |
| P2 | 后端 Receipt transaction refs 仍未分页；前端已 display pagination，保留为已接受 Contract 限制。 |
| P3 | Vite 单 bundle 大于 500 kB advisory；本阶段不做 code splitting。 |

## Scope confirmation

变更仅包括 frontend production hardening、frontend unit/E2E tests 与本 Closing Review。backend、migration、Frozen Contract、Architecture、`sites-demo` 和 `AGENTS.md` 未修改；`AGENTS.md` 保持未跟踪且不纳入提交。

## 下一步准入

可以交给 **Sol，高思考** 执行 Transaction Import Frontend A–D 最终独立只读 Closing Review。该配置适合核验跨阶段金融权威、恢复与并发不变量；此时无需更高档位，除非独立审查发现架构级问题。完成该审查后无需常规二次模型复审。
