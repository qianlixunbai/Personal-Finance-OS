# Decisions

このディレクトリは、軽量な意思決定記録のために予約されています。

このプロジェクトには、正式な ADR ディレクトリとして `docs/ADR/` がすでに存在します。

利用方針:

- 重大で長期的に有効な技術判断は `docs/ADR/` に記録する。
- 一時的または作業過程の判断は、まず `docs/logs/` または `docs/review/` に記録する。
- 一時的な判断が長期的な制約になった場合は、ADR として整理する。
